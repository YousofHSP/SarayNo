@using Common.Utilities
@using Domain
@model List<Payoff>

@{
    ViewBag.Title = "پرداختی های مستقیم کارفرما";
    Layout = "_Layout";
    var projects = ViewBag.Projects as List<Project>;
}

<div class="card">
    <div class="card-body">
        <h3>پرداختی های مستقیم کارفرما</h3>

        @if (projects is not null && projects.Any())
        {
            <div class="row gap-2">
                @foreach (var item in projects)
                {
                    <a asp-action="DirectPayment" asp-route-projectId="@item.Id" class="card"
                       style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">@item.Title</h5>
                            <h6 class="card-subtitle mb-2 text-muted">@(item.User.FirstName + " " + item.User.LastName)</h6>
                            @* <p class="card-text">@project.</p> *@
                        </div>
                    </a>
                }
            </div>
        }
        else
        {
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>تاریخ</th>
                    <th>توضیحات</th>
                    <th>مبلغ</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var item in Model.OrderBy(i => i.Date))
                {
                    <tr>
                        <td>@item.Date.ToShamsi()</td>
                        <td>@item.Description</td>
                        <td>@item.Price.ToNumeric()</td>
                    </tr>
                }
                </tbody>
                <tfoot>
                <tr>
                    <td colspan="2"></td>
                    <td>@(Model.Sum(i => i.Price).ToNumeric())</td>
                </tr>
                </tfoot>
            </table>
        }
    </div>
</div>

